#!/usr/bin/env bash

# This can be used as an alternative entry point or for additional setup
CURRENT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

# Source the main plugin
source "$CURRENT_DIR/toggle-panel.tmux"

